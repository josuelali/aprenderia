:root{
  --bg: #0b1020;
  --panel: #0f172a;
  --ink: #e5e7eb;
  --muted: #9ca3af;
  --blue: #22d3ee;
  --violet: #7c7cff;
  --steel: #94a3b8;
  --card: #0b122a;
  --line: rgba(148,163,184,.15);
}

*{ box-sizing:border-box; }
html,body{ margin:0; padding:0; }
body{
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Inter, sans-serif;
  background: radial-gradient(1200px 600px at 50% -10%, #0f1b3d 0%, #060912 45%, #05060b 100%);
  color: var(--ink);
}

a{ color: var(--blue); text-decoration:none; }
.container{
  max-width: 1100px;
  margin: 0 auto;
  padding: 56px 20px;
}

/* HERO */
.hero{
  position: relative;
  overflow: hidden;
  border-bottom: 1px solid var(--line);
}
.grid{
  position:absolute; inset:0;
  background:
    linear-gradient(transparent 95%, rgba(124,124,255,.08) 96%) 0 0/40px 40px,
    linear-gradient(90deg, transparent 95%, rgba(34,211,238,.08) 96%) 0 0/40px 40px;
  opacity:.25;
}
.hero-content{
  position: relative;
  text-align:center;
}
.badge{
  display:inline-block;
  padding:6px 14px;
  border-radius:999px;
  background: linear-gradient(90deg, rgba(34,211,238,.15), rgba(124,124,255,.15));
  border:1px solid var(--line);
  color:#c7d2fe;
  margin: 8px 0 18px;
  font-size:.85rem;
}
h1{
  font-size: clamp(2.2rem, 4vw, 3.1rem);
  line-height:1.05;
  margin: 10px 0 14px;
  background: linear-gradient(90deg, #e5e7eb, #a5b4fc);
  -webkit-background-clip:text;
  background-clip:text;
  color: transparent;
}
.subtitle{
  max-width: 760px;
  margin: 0 auto 22px;
  color: var(--muted);
  font-size: 1.05rem;
}
.cta{ display:flex; gap:12px; justify-content:center; flex-wrap:wrap; }
.btn{
  padding:12px 18px;
  border-radius:12px;
  border:1px solid var(--line);
  font-weight:600;
}
.btn.primary{
  background: linear-gradient(90deg, var(--blue), var(--violet));
  color:#020617;
}
.btn.ghost{
  background: transparent;
  color: var(--ink);
}

/* ROBOT */
.robot-core{
  position: relative;
  display:inline-grid;
  place-items:center;
  margin: 10px auto 6px;
}
.robot{
  font-size: 4rem;
  filter: drop-shadow(0 8px 24px rgba(124,124,255,.35));
}
.halo{
  position:absolute;
  width:120px; height:120px;
  border-radius:50%;
  background: radial-gradient(circle, rgba(34,211,238,.35), rgba(124,124,255,.15), transparent 60%);
  filter: blur(6px);
}

/* CARDS */
.card{
  background: linear-gradient(180deg, rgba(255,255,255,.02), rgba(255,255,255,0));
  border:1px solid var(--line);
  border-radius:18px;
  padding:28px;
  margin-bottom:26px;
}
.card.accent{
  border-color: rgba(34,211,238,.35);
  box-shadow: inset 0 0 0 1px rgba(34,211,238,.08);
}
h2{
  margin:0 0 12px;
  font-size:1.5rem;
  color:#e5e7eb;
}

/* CHECKS */
.checks{
  list-style:none;
  padding:0;
  margin:10px 0 0;
}
.checks li{
  position:relative;
  padding-left:28px;
  margin-bottom:10px;
  color:#cbd5f5;
}
.checks li::before{
  content:"▸";
  position:absolute; left:0; top:0;
  color: var(--blue);
}

/* FOOTER */
.footer{
  border-top:1px solid var(--line);
}
.footer-inner{
  display:flex; gap:12px; justify-content:space-between; align-items:center;
  color: var(--steel);
}
.footer a{ color: var(--steel); margin-left:12px; }

@media (max-width:640px){
  .footer-inner{ flex-direction:column; }
.checks{
  max-width: 520px;
  margin: 14px auto 0;
}

}
