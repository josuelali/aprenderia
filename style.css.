* {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  background: radial-gradient(circle at top, #0f172a, #020617);
  color: #e5e7eb;
}

/* HERO */
.hero {
  min-height: 100vh;
  text-align: center;
  padding: 80px 20px;
  position: relative;
  overflow: hidden;
}

.hero::before {
  content: "";
  position: absolute;
  inset: 0;
  background: radial-gradient(600px 300px at 50% 0%, rgba(34,211,238,.18), transparent 60%);
  pointer-events: none;
}

.robot {
  font-size: 64px;
  margin-bottom: 10px;
}

.badge {
  display: inline-block;
  background: rgba(34,211,238,.15);
  color: #67e8f9;
  padding: 8px 18px;
  border-radius: 30px;
  font-size: 0.85rem;
  margin-bottom: 25px;
}

h1 {
  font-size: 3rem;
  margin: 20px 0;
  color: #f8fafc;
}

.subtitle {
  max-width: 700px;
  margin: auto;
  font-size: 1.1rem;
  color: #cbd5f5;
}

.cta {
  margin-top: 40px;
}

.btn {
  display: inline-block;
  padding: 14px 28px;
  margin: 8px;
  border-radius: 8px;
  text-decoration: none;
  font-weight: 600;
}

.primary {
  background: linear-gradient(135deg, #22d3ee, #3b82f6);
  color: #020617;
}

.secondary {
  border: 1px solid #38bdf8;
  color: #38bdf8;
}

/* SECCIONES */
.section {
  padding: 80px 20px;
  max-width: 900px;
  margin: auto;
}

.section.dark {
  background: #020617;
}

h2 {
  text-align: center;
  font-size: 2rem;
  margin-bottom: 30px;
}

p {
  text-align: center;
  max-width: 700px;
  margin: auto;
  color: #cbd5f5;
}

.checks {
  list-style: none;
  padding: 0;
  max-width: 520px;
  margin: 30px auto 0;
}

.checks li {
  margin: 14px 0;
  padding-left: 30px;
  position: relative;
}

.checks li::before {
  content: "✔";
  position: absolute;
  left: 0;
  color: #22d3ee;
}

/* FOOTER */
footer {
  text-align: center;
  padding: 40px 20px;
  font-size: 0.85rem;
  color: #94a3b8;
}

footer a {
  color: #38bdf8;
  text-decoration: none;
  margin: 0 6px;
}
